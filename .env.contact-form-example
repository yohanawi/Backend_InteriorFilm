# ============================================
# CONTACT FORM CONFIGURATION
# ============================================
# Add these settings to your .env file

# Mailtrap SMTP Settings (Development)
MAIL_MAILER=smtp
MAIL_HOST=sandbox.smtp.mailtrap.io
MAIL_PORT=587
MAIL_USERNAME=2b9fda5c502e8b
MAIL_PASSWORD=Yoahn@123
MAIL_ENCRYPTION=tls
MAIL_FROM_ADDRESS=noreply@interiorfilm.com
MAIL_FROM_NAME="${APP_NAME}"

# Admin Email for Contact Form Notifications
ADMIN_EMAIL=info@xesstrading.com

# Queue Configuration (Required for background email sending)
QUEUE_CONNECTION=database

# ============================================
# PRODUCTION SMTP (Replace Mailtrap values above)
# ============================================
# MAIL_HOST=smtp.your-provider.com
# MAIL_PORT=587
# MAIL_USERNAME=your-smtp-username
# MAIL_PASSWORD=your-smtp-password
